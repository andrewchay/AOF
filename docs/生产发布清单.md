# AOF 生产发布清单

适用范围：
- `/Users/chaihao/LLM/AOF/services/semantic_middle_layer_api`
- 中间层构建链路（ontology + mapping + regression + skills suggestions）

## 1. 发布版本信息（必须冻结）

- [ ] 发布日期：`YYYY-MM-DD`
- [ ] AOF git commit：`<commit>`
- [ ] 镜像 tag：`aof-semantic-api:<tag>`
- [ ] `COGNEE_PIP_SPEC`：`cognee==x.y.z` 或 `git+...@<commit>`
- [ ] Python 版本：`3.13.x`

## 2. 构建前检查

- [ ] 一键预检通过：
  - `bash tools/ci/pre_release_check.sh`
- [ ] 治理门禁通过（无待审批变更单）：
  - `python tools/governance/validate_release_gate.py --root data/governance --strict`
  - （可选）按 topic 发布时：`python tools/governance/validate_release_gate.py --root data/governance --strict --topic <topic>`

- [ ] 关键脚本语法检查通过：
  - `bash -n tools/middle_layer/run_semantic_middle_layer.sh`
  - `bash -n services/semantic_middle_layer_api/run_api.sh`
- [ ] Python 编译检查通过：
  - `python -m py_compile services/semantic_middle_layer_api/app.py`
  - `python -m py_compile tools/middle_layer/build_semantic_middle_layer.py`
- [ ] OpenAPI 与实现端点一致（至少核心端点）：
  - ingest/build/artifacts/semantic/export

## 3. 镜像构建

```bash
cd /Users/chaihao/LLM/AOF
docker build \
  -f services/semantic_middle_layer_api/Dockerfile \
  --build-arg INSTALL_COGNEE=1 \
  --build-arg COGNEE_PIP_SPEC="cognee==0.1.45" \
  -t aof-semantic-api:<tag> .
```

- [ ] 构建日志无错误
- [ ] 镜像扫描通过（高危漏洞为 0）

## 4. 部署参数（生产）

必需环境变量：
- [ ] `AOF_ROOT=/app/AOF`
- [ ] `LLM_API_KEY=<prod_key>`

建议环境变量：
- [ ] `COGNEE_ROOT=<path_or_empty>`（对齐日志解析场景）
- [ ] `PYTHONUNBUFFERED=1`

卷挂载：
- [ ] `/app/AOF/data`
- [ ] `/app/AOF/logs`
- [ ] `/app/AOF/ontologies`

## 5. 发布后 5 分钟验收（Smoke Test）

- [ ] `GET /healthz` 返回 `{"status":"ok"}`
- [ ] 打开 `/docs` 可访问
- [ ] 调用链路最小闭环：
  1. `POST /v1/ingest/docs`
  2. `POST /v1/ingest/metadata`
  3. `POST /v1/build/topic`（可先 `skip_align=true`）
  4. `GET /v1/artifacts/{run_id}`
- [ ] 产物生成成功：manifest + mapping + regression

## 6. 发布后 30 分钟业务验收

- [ ] 选 1 个真实 topic 跑完整中间层构建
- [ ] 下游 Text-to-SQL 读取 mapping 库成功
- [ ] 至少 3 条回归样例执行通过
- [ ] feedback 注入后能生成新 feedback candidates 与 skills_update

## 7. 回滚策略（必须提前准备）

- [ ] 保留上一个稳定镜像 tag：`aof-semantic-api:<prev_tag>`
- [ ] 回滚命令已验证
- [ ] 回滚后 smoke test 清单已准备

回滚示例：
```bash
docker stop aof-semantic-api || true
docker rm aof-semantic-api || true
docker run -d --name aof-semantic-api -p 8787:8787 \
  -e AOF_ROOT=/app/AOF \
  -e LLM_API_KEY="$LLM_API_KEY" \
  -v $(pwd)/data:/app/AOF/data \
  -v $(pwd)/logs:/app/AOF/logs \
  -v $(pwd)/ontologies:/app/AOF/ontologies \
  aof-semantic-api:<prev_tag>
```

## 8. 监控与告警（最低要求）

- [ ] API 5xx 比例告警
- [ ] `/healthz` 探活失败告警
- [ ] 构建失败率告警（`/v1/build/topic`）
- [ ] 日志目录容量告警（`logs/`）

## 9. 安全与合规

- [ ] API Key 不写入仓库与镜像
- [ ] feedback/metadata 输入日志脱敏
- [ ] 对外环境加鉴权（网关或服务内 API Key）
- [ ] 关键操作（build/export）保留审计日志

## 10. CI 门禁建议

- [ ] Lint + 类型检查
- [ ] 单元测试（API 层）
- [ ] 集成测试（ingest->build->artifacts）
- [ ] 回归样例抽检（至少 1 个 topic）
- [ ] Docker 镜像构建与启动测试

## 11. 发布责任人

- [ ] 发布执行：
- [ ] 业务验收：
- [ ] 回滚批准人：
- [ ] 值班人（24h）：
