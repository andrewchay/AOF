# AOF 架构：受治理的语义运行时

> **状态**：架构真值源（实现对照）
>
> **更新日期**：2026-08-30
> **边界**：本文描述仓库中已实现或已有明确接口的架构机制；不将尚未完成真实业务闭环验证的能力表述为已证明的产品效果或生产承诺。

## 1. 架构意图

AOF（Agentic Ontology Factory）是一个受治理的企业语义运行时平台：它把企业知识、业务口径与规则转化为可运行的语义资产，并让人和 Agent 在明确的证据、版本、权限与审批边界内进行查询、推理、仿真、建议与受控动作。

它不是单纯的知识图谱或 Text-to-SQL 工具：图谱、文档摄取、检索、导出与 SQL mapping 都是语义资产的输入、承载或消费面。AOF 的核心是一个**受治理的语义运行时**。

“本体工厂”保留为语义资产构建、诊断和实施能力；它不再是终局产品定位。平台的第一个可审计证明点是财务控制平面：ERP 保持正式账本，AOF 提供其上方的控制、行动编排、过程证据与价值归因层。详见 [运行时平台定位与财务控制平面](docs/product/AOF_运行时平台定位与财务控制平面_v2.md)。

## 2. 已实现的能力边界

| 能力 | 架构事实 | 当前不应推断的结论 |
|---|---|---|
| 语义资产治理 | 支持 draft、校验、评审、发布、豁免与不可变 release 清单 | 不等于所有领域本体已完成治理或已被客户采用 |
| 语义运行 | 提供 canonical Semantic IR、确定性推理、查询与编译重放 | 不等于任何回答天然正确或所有过程资产已产品化 |
| 受控动作 | 动作目录、计划、策略、控制平面、执行记录、补偿及仿真接口已存在 | 不等于已在真实业务系统完成端到端自动执行 |
| 决策证据 | 提供 append-only、哈希链的决策记录、证据关联、审计与先例检索 | 不等于已形成决策孪生或效果归因飞轮 |
| 接入与适配 | MCP、CLI/API、摄取、图后端、导出与中间层产物可作为消费面 | 不等于所有部署、弹性与多租户目标已在生产证明 |

## 3. 总体架构

```text
人 / Agent / 业务系统
        │
        ▼
接入与治理边界 ── 身份、租户、权限、审计、MCP/API/CLI
        │
        ▼
语义资产生命周期 ── 录入 → Draft → Validate → Review → Publish → Release
        │                                         │
        │                                         └── 不可变修订清单与回放锚点
        ▼
语义运行时 ── Canonical Semantic IR、映射、规则、推理、查询/编译重放
        │
        ├──────────────────────────┐
        ▼                          ▼
仿真与行动控制                 决策证据与过程资产
Scenario → Plan → Authorize     Evidence → Decision → Audit / Precedent
→ Execute → Compensate          （未来：期望模型与偏差检测）
        │                          │
        └──────────────┬───────────┘
                       ▼
适配与承载层 ── 文档/元数据/反馈、图存储、SQL/BI、导出、外部连接器
```

### 核心原则

1. **语义先于生成**：Agent 的自然语言输入不能绕过已发布的语义资产、规则与控制边界。
2. **发布先于运行**：可运行的语义修订必须可定位到版本和 release。
3. **建议与执行分离**：生成计划、批准、实际执行、补偿与对账是不同状态；高风险动作不能被建议直接触发。
4. **证据与结果可追溯**：重要决策需要关联输入证据、执行轨迹与后续结果，而非只保存文本结论。
5. **声明态与运行态可对账**：导出的本体/OKF 等声明资产与图谱、mapping、规则等运行资产必须能比较漂移；该对账门禁仍是待补齐能力。

## 4. 核心层

### 4.1 接入与治理边界

外部入口包括 MCP、CLI、Web/REST API 与 SDK。入口层负责身份、权限、租户解析、审计及请求控制；它不承担业务语义的最终裁判。

MCP 是 Agent 的受控消费面，包含语义查询/编译重放、决策记录与审计、ontology draft 生命周期、Datalog 推理等工具。工具能力必须由其后端契约、发布状态和授权策略约束，而非由提示词单独约束。

### 4.2 语义资产生命周期

语义资产来自文档、元数据、历史 SQL、业务反馈及人工建模。中间层可产出 OWL、术语/指标/维度 mapping、SQL pattern、回归样例与产物清单；这些产物是可审阅的语义输入，而非直接等同于可信运行结论。

本体治理工作流将修订置于明确状态中：

```text
create draft → validate → request changes / approve → publish → release
                              │
                              └── finding waiver（有记录的例外）
```

`KnowledgeRelease` 将一组可运行的资源修订锚定为不可变清单，使查询、推理、仿真和动作能够引用明确的语义版本。

### 4.3 语义运行时

语义运行时负责把已发布资产转化为可计算、可复现的业务解释与约束，主要包括：

- canonical Semantic IR 与受控的语义编译；
- 术语、指标、维度和 SQL pattern 等 mapping；
- Datalog 规则集的发布与确定性推理；
- 语义查询与编译重放，用于检查当时的输入、版本及产生过程。

图谱后端、全文/语义检索和 Text-to-SQL 是该层的适配或消费能力。它们必须受定义、过滤条件、血缘、回归样例和发布版本约束；不应成为 AOF 的唯一架构中心。

### 4.4 仿真、动作与控制

受控动作路径将语义解释转化为明确、可审计的行动对象：

```text
Semantic IR / released rules
  → Action Contract / Catalog
  → governed Action Plan + policy evaluation
  → authorization / control-plane decision
  → Action Run via connector
  → outcome, compensation, or reconciliation-required
```

`SimulationRequest` 与 bitemporal 快照支持无副作用的情景推演；它用于在执行前比较假设和影响，不构成真实世界效果的保证。

执行服务须记录运行状态与事件；出现不确定结果或补偿失败时，应冻结在 `reconciliation_required`，而不是将重试伪装成已经恢复。AI 参与级别、风险分类与人工确认门是动作元数据的后续强化方向，尚未作为统一的全局能力完成。

### 4.5 决策证据与过程资产

决策溯源层以 `DecisionRecord` 和 `EvidenceRef` 记录证据、决策、上下文及关联关系，采用 append-only 与哈希链保护历史完整性，并支持审计轨迹和先例检索。

它的当前职责是证明“为何做出该决策、基于什么证据、之后如何被引用”。下一阶段可在此基础上形成决策孪生：对比期望决策路径与实际遥测，发现跳过核验、反复被推翻的先例或未被消费的结论。该能力是路线图，不应被写作已实现。

## 5. 数据与控制流

### 5.1 从知识到可运行语义

```text
文档 / 元数据 / SQL / 专家反馈
  → 语义抽取与人工审阅
  → OWL、mapping、回归样例、规则
  → draft 校验与治理决策
  → publish + immutable release
  → Semantic IR、推理、查询及受控下游消费
```

### 5.2 从业务问题到受控动作

```text
问题或事件
  → 依据 release 进行语义解释与推理
  → 记录证据和决策 / 提出动作请求
  → 计划与策略检查
  → 仿真（如适用）
  → 授权
  → 执行、审计、补偿或人工对账
  → 结果回流为新的证据、反馈或语义修订候选
```

任何箭头都不表示自动授权。尤其是从建议到执行、从执行到“成功”的跃迁，都必须由状态、策略或人工批准证据明确表达。

## 6. 存储、适配与导出

### 存储

`GraphBackend` 抽象允许以 Cognee 或 NebulaGraph 等后端承载图数据；语义 release、动作运行和仿真也有文件或 SQLite 持久化实现。存储选型不改变语义治理与动作控制的契约。

### 适配器

输入适配器处理文档、表格、SQL、元数据和 `feedback.jsonl`。下游适配器可面向 Text-to-SQL、BI Copilot、指标治理、查询审计、语义检索、规则引擎或业务连接器。每个适配器必须暴露来源、版本、权限与失败状态，而不允许绕过发布和控制边界。

### 导出

Markdown/OKF 等导出面服务于人类审阅、Git 协作、迁移和 Agent 的渐进式消费。导出物是声明态的可读投影，不应被误用为运行态事实；二者的自动漂移对账仍待建设。

## 7. 扩展规则

| 新增内容 | 必须回答的问题 |
|---|---|
| 新领域本体或 mapping | 来源、负责人、变更流程、验证与 release 如何定义？ |
| 新规则或推理 | 输入语义版本、确定性边界、回放与失败解释是什么？ |
| 新 MCP 工具或 Agent Skill | 它能读取、建议还是执行？所需权限、审计与人工确认是什么？ |
| 新动作连接器 | 契约、幂等性、授权、结果证据、补偿与不确定结果处置是什么？ |
| 新存储或导出 | 是否保留版本锚点、血缘和声明态/运行态对账能力？ |

## 8. 验证路线与非目标

当前最重要的架构验证不是再增加一层通用组件，而是完成一个被命名、可度量的真实业务闭环。首个证明点是**财务控制平面**：以一个高频财务例外流程为范围，贯穿正式数据、财务语义与 Policy Pack、影响推演、审批、受控系统动作、结果对账和 Impact Ledger。指标口径治理与 SQL 回归仍是必要的底层控制能力，但不再充当产品叙事中心。

在该验证之前，以下内容均不应作为已完成事实写入架构承诺：

- AOF 已被验证为 L3 操作本体或 L4 智能体本体；
- 仿真预测已带来真实业务收益；
- 动作层已完成生产级自动执行；
- 多租户、Kubernetes、高可用、外部连接器及容灾已获得生产验证；
- 决策溯源已形成可量化的决策飞轮。

## 9. 相关文档

- `docs/语义中间层接口契约.md`：语义输入、产物和下游消费接口。
- `docs/aof-core-capability.md`：数字化孪生业务能力叙事；其中策略效果示例应作为目标场景，而非当前运行时的已验证效果。
- `docs/AOF架构评审-OntoEffect视角-2026-08-30.md`：本次架构重写的评审依据与待验证假设。

## 版本历史

- **v3.0（2026-08-30）**：重建为受治理语义运行时的架构真值源，区分已实现机制、待验证闭环和未证实生产能力。
- **v2.0**：以摄取、图谱服务、导出与企业基础设施为中心的架构描述；保留历史参考价值，但不再作为当前能力全貌。
